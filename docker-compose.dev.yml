# Optional developer-only host ports. Never use this overlay for VM or production deployments.
services:
  gateway:
    ports:
      - "${GATEWAY_PORT:-8080}:80"
  main:
    ports:
      - "${MAIN_SERVICE_PORT:-8000}:8000"
  research:
    ports:
      - "${RESEARCH_SERVICE_PORT:-8001}:8001"
  library:
    ports:
      - "${LIBRARY_SERVICE_PORT:-8002}:8002"
