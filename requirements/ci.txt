# Phase 0 verification tools. Pin exact versions so CI behavior changes only
# through reviewed dependency updates.
detect-secrets==1.5.0
pip-audit==2.9.0
PyYAML==6.0.3
ruff==0.15.22
pytest==9.1.1
pytest-asyncio==1.4.0
