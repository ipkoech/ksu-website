# The host topology reaches HERI through the gateway. The server variable is
# also used by standalone builds and takes precedence over the browser value.
KSU_HERI_API_URL=http://localhost:8080
NEXT_PUBLIC_HERI_API_URL=http://localhost:8080/api/v1/heri
NEXT_BASE_PATH=/heri-africa
ADMIN_FRONTEND_ORIGIN=http://localhost:3001
