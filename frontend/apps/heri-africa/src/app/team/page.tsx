import { SiteShell } from "../../components/site-shell";
import { getTeam } from "../../lib/api";

export default async function TeamPage() { const team = await getTeam().catch(() => []); return <SiteShell><main className="mx-auto max-w-7xl px-6 py-20"><p className="text-sm font-semibold uppercase tracking-[0.16em] text-heri-teal">People behind the research</p><h1 className="mt-4 text-5xl font-semibold text-heri-blue">African expertise. Shared purpose.</h1><div className="mt-12 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{team.map((member) => <article className="rounded-3xl bg-white p-6 ring-1 ring-heri-teal/10" key={member.id}>{member.photo_url && <img alt={member.name} className="aspect-square w-full rounded-2xl object-cover" src={member.photo_url} />}<h2 className="mt-5 text-xl font-semibold text-heri-blue">{member.name}</h2><p className="mt-1 text-sm font-semibold text-heri-teal">{member.role}</p><p className="mt-3 text-sm leading-7 text-heri-ink/70">{member.biography}</p></article>)}</div></main></SiteShell>; }
